# 010 写真インポート — UI

`/import` ページの実装。写真アップロード、解析結果表示、一括お気に入り追加。

依存: `003` `004` `005` `009`

## ToDo

- [x] `app/import/page.tsx` を作成する（`"use client"`）
- [x] カメラ撮影 / ファイル選択の両方に対応するアップロードUIを実装する
- [x] アップロード後に画像を base64 に変換して `/api/import` へ POST する
- [x] 解析中はローディングスピナーと「解析中...」メッセージを表示する
- [x] 検出結果を一覧表示する（チェックボックス付き）
- [x] 各検出アイテムに対して Open Library / Last.fm API で照合し、正式情報を補完する
- [x] 照合成功アイテムにはカバー画像・タイトル・著者/アーティストを表示する
- [x] 照合失敗アイテムを明示し、検索画面へ遷移するリンクを表示する
- [x] 選択アイテムをまとめて「追加する」ボタンで `favorites.ts` の追加関数を呼ぶ
- [x] 追加完了後にマイコレクション（`/my`）へ遷移する
