# 011 UI仕上げ・デプロイ

共通コンポーネントの整備、UXの細かい調整、Vercel へのデプロイ。

依存: `006` `007` `008` `010`

## ToDo

### 共通コンポーネント
- [x] `components/ItemCard.tsx` を作成・共通化する（ホーム・検索・マイコレクションで再利用）
- [x] `components/Placeholder.tsx` を作成する（タイトル頭文字＋背景色）

### UX調整
- [x] 各ページの画像読み込みエラー時にプレースホルダーへフォールバックする処理を追加する
- [x] モバイル表示でナビゲーションが使いやすいことを確認する
- [x] ページ遷移時のローディング状態を整える

### デプロイ
- [x] Vercel プロジェクトを作成し、リポジトリと連携する
- [x] Vercel の環境変数に `.env.local` の値をすべて設定する
- [x] `npm run build` がエラーなく通ることを確認する
- [x] デプロイ後に全4画面の動作を本番環境で確認する
