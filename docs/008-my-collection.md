# 008 マイコレクションページ

`/my` ページの実装。自分のお気に入り一覧・削除。

依存: `001` `002` `005`

## ToDo

- [x] `app/my/page.tsx` を作成する（`"use client"`）
- [x] マウント時に匿名UUIDを取得し、自分のお気に入りを Supabase から取得する
- [x] カード形式で一覧表示する（カバー画像・タイトル・著者/アーティスト）
- [x] 各カードに「削除」ボタンを追加し、`favorites.ts` の削除関数を呼ぶ
- [x] 削除後にリストを即時更新する（楽観的更新 or 再フェッチ）
- [x] カバー画像がない場合にプレースホルダーを表示する
- [x] `localStorage` 削除時にコレクションが引き継げない旨の注記をページ上部に表示する
- [x] データが0件の場合の空状態メッセージを表示する
